<% if activity.trackable %>
   <div class="activity-c">
      menambahkan order item
      <%= link_to activity.trackable.name, order_item_path(activity.trackable.id) %>
  </div>
<% else %>
   order item telah di hapus
<% end %>
