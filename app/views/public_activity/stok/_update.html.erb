<% if activity.trackable %>
   <div class="activity-u">
      memperbarui stok
      <%= link_to activity.trackable.nama, stok_path(activity.trackable.id) %>
   </div>
<% else %>
   stok telah di hapus
<% end %>
