<%= render 'layouts/global_nav' %>
<div class="jumbotron">
</div>
<div class="col-md-12">
<%= flash_messages %>
</div>
  
    <div class="col-md-3 col-sm-3">
      <div class="panel panel-default">
      <div class="panel-heading text-center">Menu</div>
      <!---->

      <!-- Stocks parent collapse -->
      <div class="list-group">
        <%= link_to new_kategori_path, class: "list-group-item" do %> 
          <i class="glyphicon glyphicon-plus"> Tambah Kategori</i>
          <% end %>
          <%= link_to new_stok_path, class: "list-group-item" do %> 
          <i class="glyphicon glyphicon-plus"> Tambah Stok</i>
          <% end %>         

      </div>

    </div>
    </div>
    <div class="col-md-9 col-sm-9">
      <div class="panel panel-default">
        <div class="panel-heading">
          <h3> Daftar Kategori</h3>
        </div>
        <div class="panel-body">
          <div class="row">
            <% @kategoris.each do |k| %>
              <div class="col-md-4 col-sm-4 text-center">
                <div class="panel panel-default">
                <div class="panel-body" style="height:75px; width:75x;">
                  <%= link_to (image_tag k.gambar_url(:thumb)), kategori_path(k)%>
                </div>
                <div class="panel-footer"><strong><%= k.nama %></strong></div>
              </div>
              </div>
            <% end %>
          </div>
          </div>
      </div>
    </div>


